---
title: "Project01"
output: pdf_document
---

```{r}
library(ggplot2)
library(ggmap)
df<-as.data.frame(cbind(loans$lon,loans$lat))
?get_map
map<-get_map()
map
str(map)
?ggmap 

lon<-loans$lon
lat<-loans$lat
```

```{r}
#Using GGPLOT, plot the Base World Map
mp <- NULL
mapWorld <- borders("world", colour="gray50", fill="gray50") # create a layer of borders
mp <- ggplot() +   mapWorld
mp<-mp+geom_point(aes(x=lon,y=lat),color="blue",size=3)
mp
```
```{r}
library(dplyr)
actualloan<-filter(loans,status=="paid")
View(actualloan)

```

